#
# production configuration values

# server
SERVER_HOST=${HOST}
SERVER_PORT=${PORT}

# database
DB_DRIVER=postgres
DB_HOST=${DB_HOST}
DB_PORT=${DB_PORT}
DB_NAME=prod
DB_USERNAME=${DB_USERNAME}
DB_PASSWORD=${DB_PASSWORD}
